# Enforce the version in package.json "packageManager" (use Corepack: corepack enable)
package-manager-strict=true
auto-install-peers=true
# Help @tailwindcss/node resolve a complete enhanced-resolve (avoids broken nested copies)
public-hoist-pattern[]=*enhanced-resolve*
